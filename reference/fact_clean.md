# Clean factor variables in prediction data

Clean factor variables in prediction data

## Usage

``` r
fact_clean(data.train, data.test, show.warning = T)
```

## Arguments

- data.train:

  A training dataset

- data.test:

  A testing dataset

- show.warning:

  logical
